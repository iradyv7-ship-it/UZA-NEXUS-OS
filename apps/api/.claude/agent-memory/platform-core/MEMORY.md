# Platform-core Memory Index

- [E2E HTTP tests need an SWC child process](feedback_e2e_http_tests.md) — in-process NestFactory in Vitest breaks DI (esbuild drops decorator metadata); spawn the app under `@swc-node/register` and drive it over fetch.
